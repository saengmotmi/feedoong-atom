# Feedoong Solo Extension

크롬 새 탭을 Feedoong Solo 웹 앱으로 연결하는 최소 확장입니다.

## 개발 메모

1. `newtab.js`의 `WEB_APP_URL`을 운영 주소로 교체 (기본 개발 주소: `http://localhost:5173`)
2. 크롬 `chrome://extensions`에서 개발자 모드 활성화
3. `apps/extension` 폴더를 "압축해제된 확장 프로그램 로드"로 등록
